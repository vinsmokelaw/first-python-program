# Python has several built-in **data types**, categorized into different groups based on their purpose. Here’s a breakdown:

# ### **1. Numeric Types**
# Used for numerical operations.
# - **int** → Integer values (e.g., `10`, `-5`, `1000`)
# - **float** → Decimal numbers (e.g., `3.14`, `-2.5`, `0.0`)
# - **complex** → Complex numbers (e.g., `3 + 4j`, `1 - 2j`)

# ### **2. Sequence Types**
# Used to store ordered collections.
# - **list** → Mutable collection of elements (e.g., `[1, 2, 3]`, `["apple", "banana"]`)
# - **tuple** → Immutable collection (e.g., `(10, 20, 30)`)
# - **range** → Generates a sequence of numbers (e.g., `range(1, 10)`)

# ### **3. Text Type**
# Used for text representation.
# - **str** → String of characters (e.g., `"Hello"`, `'Python'`)

# ### **4. Set Types**
# Used for storing unique values.
# - **set** → Unordered, mutable collection with unique elements (e.g., `{1, 2, 3}`)
# - **frozenset** → Immutable version of a set

# ### **5. Mapping Type**
# Used for key-value pairs.
# - **dict** → Dictionary that maps keys to values (e.g., `{"name": "Alice", "age": 25}`)

# ### **6. Boolean Type**
# Used for logical operations.
# - **bool** → Represents `True` or `False`

# ### **7. Binary Types**
# Used for binary data.
# - **bytes** → Immutable sequence of bytes
# - **bytearray** → Mutable version of bytes
# - **memoryview** → Provides memory-efficient view of bytes

# ### **8. None Type**
# Represents absence of a value.
# - **NoneType** → `None`

# Each data type is used in different scenarios based on the requirements of a program. Let me know if you need more details! 🚀